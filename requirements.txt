fastapi
uvicorn[standard]
httpx
pyyaml
python-dotenv
pandas
numpy>=1.24,<2.0
requests
pytest
pytest-asyncio
pytest-cov>=4.1

# Optional: ML/AI regime classification
lightgbm>=4.0
joblib>=1.3

# Optional: Strategy optimization
optuna>=3.0

# Optional: Extended technical indicators (130+ indicators)
pandas-ta>=0.3.14b1

# Optional: Charting/Visualization
matplotlib>=3.7
mplfinance>=0.12
seaborn>=0.12

# Optional: Reinforcement Learning
gymnasium>=0.29
stable-baselines3>=2.0  # NOTE: requires torch>=2.0; pin torch<2.5 if SB3<2.4

# Optional: Statistical analysis
statsmodels>=0.14
scipy>=1.11

# Optional: TA-Lib (requires system install: conda install -c conda-forge ta-lib)
# TA-Lib>=0.4.28

# Optional: Public market data (free, no API key required)
yfinance>=0.2
feedparser>=6.0

# Optional: News sentiment analysis
vaderSentiment>=3.3

# Optional: Advanced NLP sentiment (FinBERT)
# transformers>=4.30
# sentencepiece>=0.1

# Optional: Backtrader
backtrader>=1.9

# Optional: bt portfolio backtesting
bt>=1.0

# Optional: Deep Learning (PyTorch)
torch>=2.0

# Optional: Deep Learning (TensorFlow) - install separately
# tensorflow>=2.15

# Optional: Portfolio optimization (convex)
# cvxpy>=1.4

# Optional: Freqtrade (install separately: pip install freqtrade)
# freqtrade>=2024.1

# Optional: Zipline (install separately due to complex deps)
# zipline-reloaded>=3.0
